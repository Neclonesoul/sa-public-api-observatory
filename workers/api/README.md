# API worker

The production API is currently emitted by the hosted web Worker at `/api/v1` so catalogue pages and machine access share one deployment contract. This directory is reserved for extraction when independent scaling is justified.
