import{PageHeader,SiteShell}from"../../components/SiteShell";export default function Page(){return <SiteShell><PageHeader eyebrow="INCIDENT HISTORY" title="Incidents" description="Transport incidents remain separate from freshness observations."/><div className="page-wrap"><div className="empty-state">No incident history exists yet. The Observatory will never invent an outage to make this page look populated.</div></div></SiteShell>}
