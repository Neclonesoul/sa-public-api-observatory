import{resources}from"../../packages/catalogue/src/catalogue";const esc=(v:unknown)=>`"${String(v??"").replaceAll('"','""')}"`;export function GET(){const head=["id","name","universe","publisher_class","access_class","resource_type","documentation_url","base_url","verification_status"];const rows=resources.map(r=>[r.id,r.name,r.universe,r.publisherClass,r.accessClass,r.resourceType,r.documentationUrl,r.baseUrl??"",r.verification.status]);return new Response([head,...rows].map(row=>row.map(esc).join(",")).join("\n"),{headers:{"Content-Type":"text/csv; charset=utf-8","Access-Control-Allow-Origin":"*"}})}
