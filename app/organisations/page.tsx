import Link from "next/link";import {PageHeader,SiteShell}from"../../components/SiteShell";import{organisations,resources}from"../../packages/catalogue/src/catalogue";
export default function Page(){return <SiteShell><PageHeader eyebrow="PUBLISHERS" title="Organisations" description="Publishers and operators represented in the verified catalogue."/><div className="page-wrap resource-grid">{organisations.map(o=><article className="resource-card" key={o.id}><p className="eyebrow">{o.publisherClass.replaceAll("-"," ")}</p><h3><Link href={`/organisations/${o.slug}`}>{o.name}</Link></h3><p>{resources.filter(r=>r.organisationId===o.id).length} verified resources</p><div className="card-bottom"><a href={o.website}>Official site ↗</a></div></article>)}</div></SiteShell>}
