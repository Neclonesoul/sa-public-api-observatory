import{CatalogueExplorer}from"../../../components/CatalogueExplorer";import{PageHeader,SiteShell}from"../../../components/SiteShell";import{resources}from"../../../packages/catalogue/src/catalogue";
export function generateStaticParams(){return[...new Set(resources.flatMap(r=>r.categories))].map(slug=>({slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const selected=resources.filter(r=>r.categories.includes(slug));return <SiteShell><PageHeader eyebrow="CATEGORY" title={slug.replaceAll("-"," ")} description={`${selected.length} verified resources classified in this subject area.`}/><CatalogueExplorer resources={selected}/></SiteShell>}
