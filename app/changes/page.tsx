import{PageHeader,SiteShell}from"../../components/SiteShell";export default function Page(){return <SiteShell><PageHeader eyebrow="SCHEMA OBSERVATORY" title="Interface changes" description="Structural signatures and OpenAPI versions will produce human-readable, payload-free diffs."/><div className="page-wrap"><div className="empty-state">No independently observed schema changes yet.</div></div></SiteShell>}
